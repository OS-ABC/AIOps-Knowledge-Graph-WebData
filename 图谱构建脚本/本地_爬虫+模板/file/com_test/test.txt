这里 cat 命令用于读取指定文件的内容并打印到终端输出，后面会详细讲它的使用。
使用 cd 命令可以切换目录，在 Linux 里面使用。
你可以使用 ls -a 命令查看隐藏文件。
使用 pwd 获取当前路径。
使用 cp （copy）命令复制一个文件或目录到指定目录。
如果直接使用 cp 命令，复制一个目录的话，会出现如下错误。
使用 cat , tac 和 nl 命令查看文件。
当我们执行一个 shell 命令行时通常会自动打开三个标准文件。
ps命令用于报告当前系统的进程状态。
可以搭配kill指令随时中断、删除不必要的程序。
ps命令是最基本同时也是非常强大的进程查看命令
ls命令能够列出当前目录下的所有内容。
Fortune命令是在相同命令行中作为fortune cookies
Linux "toilet"命令
它比figlet命令的效果更有艺术感。